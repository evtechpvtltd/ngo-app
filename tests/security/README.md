# tests/security/

Reserved for security-focused test suites beyond RLS (which lives in
`supabase/tests/`): tampered-payload tests, replay tests, and
authorization-bypass regression tests. Empty in v0.0.1 — see
`docs/threat-model.md` for what's tracked but not yet covered by a test.
